# Nettoyage Atlas — 22/08/2026

Version conservee : `3.1.0-dev`.

## Supprime

- `build/` et `dist/` : sorties recreables ;
- `models/` : anciens essais Vosk/openWakeWord devenus inutiles ;
- `src/atlas.egg-info/` : metadata setuptools generee ;
- `tools/wakeword/` : outils temporaires d'enregistrement/entrainement Vosk/ONNX ;
- `main.py` et `src/atlas/ui/` : ancienne UI Python/PySide6 ;
- `MainWindow.xaml(.cs)` : ancien point d'entree WinUI ;
- `WorkspaceArcControl.xaml(.cs)` : prototype natif non utilise ;
- `.env` : volontairement exclu de l'archive pour ne pas redistribuer les secrets locaux.

## Modifie

- `pyproject.toml` : retrait de `PySide6` ;
- `.gitignore` : ignore builds, models, training, egg-info et artefacts C# ;
- `installer/atlas_core.spec` : embarque `windows_wakeword.ps1` dans Atlas.Core ;
- README WinUI actualises.

## Conserve

- Core Python et Skills ;
- Windows Speech wake word ;
- AtlasService ;
- Atlas.UI / DesktopWindow ;
- IPC et telemetry ;
- installer et tests ;
- configuration utilisateur (hors secrets `.env`).

`build/` et `dist/` seront regeneres automatiquement lors des prochains builds.
