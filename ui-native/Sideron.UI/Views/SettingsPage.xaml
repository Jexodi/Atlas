<Page
    x:Class="Sideron.UI.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer
        VerticalScrollBarVisibility="Auto"
        HorizontalScrollBarVisibility="Disabled">

        <StackPanel
            Margin="34,22,38,38"
            Spacing="22">

            <StackPanel
                Spacing="4">

                <TextBlock
                    Text="Paramètres"
                    FontFamily="Segoe UI Variable"
                    FontSize="31"
                    FontWeight="SemiBold"
                    Foreground="{StaticResource SideronTextBrush}" />

                <TextBlock
                    Text="Configuration de l’interface native"
                    FontFamily="Segoe UI Variable"
                    FontSize="13"
                    Foreground="{StaticResource SideronSecondaryTextBrush}" />

            </StackPanel>

            <Border
                Style="{StaticResource SideronCardStyle}">

                <StackPanel
                    Spacing="18">

                    <TextBlock
                        Text="CONFIGURATION ACTIVE"
                        FontFamily="Segoe UI Variable"
                        FontSize="11"
                        CharacterSpacing="150"
                        Foreground="{StaticResource SideronSecondaryTextBrush}" />

                    <Grid
                        ColumnSpacing="26"
                        RowSpacing="12">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition
                                Width="180" />

                            <ColumnDefinition
                                Width="*" />
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition
                                Height="Auto" />

                            <RowDefinition
                                Height="Auto" />

                            <RowDefinition
                                Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock
                            Grid.Row="0"
                            Text="Zone Sideron"
                            Foreground="{StaticResource SideronSecondaryTextBrush}" />

                        <TextBlock
                            x:Name="StorageValue"
                            Grid.Row="0"
                            Grid.Column="1"
                            Foreground="{StaticResource SideronTextBrush}" />

                        <TextBlock
                            Grid.Row="1"
                            Text="Écran"
                            Foreground="{StaticResource SideronSecondaryTextBrush}" />

                        <TextBlock
                            x:Name="ScreenValue"
                            Grid.Row="1"
                            Grid.Column="1"
                            Foreground="{StaticResource SideronTextBrush}" />

                        <TextBlock
                            Grid.Row="2"
                            Text="Version"
                            Foreground="{StaticResource SideronSecondaryTextBrush}" />

                        <TextBlock
                            x:Name="VersionValue"
                            Grid.Row="2"
                            Grid.Column="1"
                            Text="3.3.6-rc.1"
                            Foreground="{StaticResource SideronTextBrush}" />

                    </Grid>

                </StackPanel>

            </Border>

            <Border
                Style="{StaticResource SideronCardStyle}">

                <StackPanel
                    Spacing="18">

                    <StackPanel
                        Spacing="4">

                        <TextBlock
                            Text="MISES À JOUR"
                            FontFamily="Segoe UI Variable"
                            FontSize="11"
                            CharacterSpacing="150"
                            Foreground="{StaticResource SideronSecondaryTextBrush}" />

                        <TextBlock
                            Text="Vérifier la disponibilité d’une nouvelle version d’Sideron."
                            FontSize="12"
                            TextWrapping="Wrap"
                            Foreground="{StaticResource SideronSecondaryTextBrush}" />

                    </StackPanel>

                    <Grid
                        ColumnSpacing="26"
                        RowSpacing="12">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition
                                Width="180" />

                            <ColumnDefinition
                                Width="*" />
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition
                                Height="Auto" />

                            <RowDefinition
                                Height="Auto" />

                            <RowDefinition
                                Height="Auto" />

                            <RowDefinition
                                Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock
                            Grid.Row="0"
                            Text="Canal"
                            Foreground="{StaticResource SideronSecondaryTextBrush}" />

                        <ComboBox
                            x:Name="UpdateChannelComboBox"
                            Grid.Row="0"
                            Grid.Column="1"
                            MinWidth="170"
                            SelectionChanged="UpdateChannelComboBox_SelectionChanged" />

                        <TextBlock
                            Grid.Row="1"
                            Text="État"
                            Foreground="{StaticResource SideronSecondaryTextBrush}" />

                        <TextBlock
                            x:Name="UpdateStatusValue"
                            Grid.Row="1"
                            Grid.Column="1"
                            Text="Non vérifié"
                            TextWrapping="Wrap"
                            Foreground="{StaticResource SideronTextBrush}" />

                        <TextBlock
                            Grid.Row="2"
                            Text="Version disponible"
                            Foreground="{StaticResource SideronSecondaryTextBrush}" />

                        <TextBlock
                            x:Name="AvailableVersionValue"
                            Grid.Row="2"
                            Grid.Column="1"
                            Text="—"
                            Foreground="{StaticResource SideronTextBrush}" />

                        <TextBlock
                            Grid.Row="3"
                            Text="Source"
                            Foreground="{StaticResource SideronSecondaryTextBrush}" />

                        <TextBlock
                            x:Name="UpdateSourceValue"
                            Grid.Row="3"
                            Grid.Column="1"
                            Text="Non configurée"
                            TextWrapping="Wrap"
                            Foreground="{StaticResource SideronTextBrush}" />

                    </Grid>

                    <StackPanel
                        Orientation="Horizontal"
                        Spacing="12">

                        <ProgressRing
                            x:Name="UpdateProgress"
                            Width="20"
                            Height="20"
                            IsActive="False"
                            Visibility="Collapsed" />

                    </StackPanel>

                    <TextBlock
                        x:Name="UpdateNotesValue"
                        Text=""
                        TextWrapping="Wrap"
                        FontSize="12"
                        Foreground="{StaticResource SideronSecondaryTextBrush}"
                        Visibility="Collapsed" />

                </StackPanel>

            </Border>

        </StackPanel>

    </ScrollViewer>

</Page>
